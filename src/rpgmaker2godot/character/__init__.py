"""Character spritesheets conversion (RPG Maker characters → Godot SpriteFrames).

Character spritesheets intentionally do not follow RPG Maker's
character sheet format: the animations (including ``idle`` rows,
which RPG Maker does not have) are natively laid out inside the image
according to the fixed layout described in :mod:`.layout`.
"""
